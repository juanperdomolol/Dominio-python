#Unir listas
#Generar primero una lista con los números entre 0 y 10, 
# luego generar otra lista con los números del 11 al 20. 
# Unir ambas lista e imprimir el resultado.
listaA = list(range(11))
listaB = list(range(10,21))
print(listaA+listaB)